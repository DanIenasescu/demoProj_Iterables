# Given an array arr containing only 0s, 1s, and 2s. Sort the array in ascending order.
import random
random_list = list(range(7))
random.shuffle(random_list)
orr = list(range(7))

print(random_list)

for index in list(range(len(random_list))):
    print(index)